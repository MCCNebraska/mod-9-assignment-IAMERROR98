import React from 'react';
import Header from '../components/header';

const NotFound = (props) => {
    return(
        <>
        <Header title= "You are not where you should be"/>
        <h1>Are you lost?</h1>
        <h3>escape while you still can...</h3>
        </>
    );
};

export default NotFound